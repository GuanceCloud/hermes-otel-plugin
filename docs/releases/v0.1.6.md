# v0.1.6

## Summary

`v0.1.6` narrows token usage tags to `llm` spans and cleans up the GitHub release publishing flow.

## What's Included

- Removed token usage fields from `hermes_request` and `invoke_agent`
- Kept `usage_*` and `gen_ai.usage.*` on `llm` spans
- Updated the Chinese and English trace and semantic mapping docs
- Published curated GitHub release notes from `release-vX.Y.Z-notes.md`

## Validation

- `python -m unittest discover -s tests -v`
- `python -m compileall src tests`
